import { ExerciseNameValidatorDirective } from './exercise-name-validator.directive';

describe('ExerciseNameValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new ExerciseNameValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
