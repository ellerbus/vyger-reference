import { AddListGroupItemDirective } from './add-list-group-item.directive';

describe('AddListLinkDirective', () =>
{
    it('should create an instance', () =>
    {
        const directive = new AddListGroupItemDirective();
        expect(directive).toBeTruthy();
    });
});
